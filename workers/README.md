# Workers Project

This directory contains the dedicated worker services for the communication platform.

## Structure
- `email-worker/`: email queue processor package
- `sms-worker/`: SMS queue processor package

## Available commands

- `npm run start:email` - start the email worker
- `npm run start:sms` - start the SMS worker